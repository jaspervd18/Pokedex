<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="favorite"
            type="com.example.pokedex.database.favorites.DatabaseFavorite" />

        <variable
            name="clickListener"
            type="com.example.pokedex.screens.favorites.FavoritesListener" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:background="@drawable/layout_border"
        android:onClick="@{()->clickListener.onClick(favorite)}">

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_marginStart="16dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:contentDescription="@string/img_description"
            app:imageUrl="@{favorite.pokemonImgUrl}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/bulbasaur"
            tools:ignore="ImageContrastCheck" />

        <TextView
            android:id="@+id/pokemon_name_textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/archivo_black"
            app:layout_constraintBottom_toBottomOf="@+id/imageView"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.092"
            app:layout_constraintStart_toEndOf="@+id/imageView"
            app:layout_constraintTop_toTopOf="@+id/imageView"
            app:layout_constraintVertical_bias="0.032"
            app:pokemonNameString="@{favorite}"
            tools:ignore="TextContrastCheck" />

        <TextView
            android:id="@+id/pokemon_nr_textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            app:layout_constraintStart_toStartOf="@+id/pokemon_name_textView"
            app:layout_constraintTop_toBottomOf="@+id/pokemon_name_textView"
            app:pokemonNrString="@{favorite}" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>